org.gradle.java.home=/Library/Java/JavaVirtualMachines/temurin-17.jdk/Contents/Home
org.gradle.jvmargs=-Xmx4G -XX:MaxMetaspaceSize=2G -Dfile.encoding=UTF-8
android.useAndroidX=true
kotlin.code.style=official

# Arc 11 / J1: the design resources and the screen helpers moved to `:sn-screen`; `:app`'s R
# must keep seeing them, so the R class stays transitive (AGP 8.11 defaults it to non-transitive).
android.nonTransitiveRClass=false
